Run this script with the following so that
chrome can have propper memory allocation
and mahimahi can have unrestricted network access:

docker run --privileged --device=/dev/net/tun --shm-size=4g -it mahimahi-chrome


Run this command to run the comprehensive script (make sure to enable permissions first):
./run_script.sh
